import styled from "styled-components";

export const Container = styled.div`
    position:fixed;
    left: 2em;
    top: 34%;
    padding: 0.5em 0.2em;
    z-index: 2;
    display: flex;
    flex-direction: column;
    gap: 2em;
    align-items: center;
    ul{
        display: flex;
        flex-direction: column;
        gap: 0.9em;
        font-size: 1.06em;
        color: #b7adad;
    }
`